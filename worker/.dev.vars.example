# PureGit Worker 本地开发环境变量模板
#
# 使用方法：复制为 worker/.dev.vars 后填写（.dev.vars 已被 .gitignore 忽略，勿提交）
#   copy worker/.dev.vars.example worker/.dev.vars
#
# 本地值必须与 vite.config.ts 的 host/port 严格同步（127.0.0.1:5173），
# 与 .github OAuth App 的 Authorization callback URL 精确一致。

GITHUB_CLIENT_ID=<你的 GitHub OAuth App Client ID>
GITHUB_CLIENT_SECRET=<你的 GitHub OAuth App Client Secret>

# 本地 OAuth 回调：GitHub OAuth App 的 Authorization callback URL 必须与此精确一致
GITHUB_OAUTH_CALLBACK=http://127.0.0.1:5173/$auth/callback
# 前端地址：登录成功后跳转目标 + CORS 允许源
FRONTEND_URL=http://127.0.0.1:5173

# ---- 生产部署时改为（同时更新 GitHub OAuth App 的回调 URL）----
# GITHUB_OAUTH_CALLBACK=https://<你的域名>/$auth/callback
# FRONTEND_URL=https://<你的域名>
# 生产密钥建议用 `wrangler secret put GITHUB_CLIENT_SECRET` 注入，勿写入 .dev.vars 提交
