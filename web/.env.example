# Worker 地址（/$auth 与 git 镜像端点所在服务）
# - 留空（默认）：前端与 Worker 同域部署（生产 Cloudflare 同域、本地 vite proxy 双进程 dev 均同源）→ 直接请求相对路径
# - 仅当 Worker 独立部署在跨域地址时设置（如前端不走 proxy、Worker 裸跑 8787）：
# VITE_WORKER_URL=http://localhost:8787
VITE_WORKER_URL=
