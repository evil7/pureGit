# 06-详情与深层页（Issue/PR/Discussion 详情 / 新建 / blob / 编辑 / 设置，2026-08-09 重建）

> 统一结论：详情/深层页**无页面级搜索框**（仅 Job 日志 Search logs、blob Go to file + Search this repository）；均已 PageLayout 收编（2026-08-09）。

## 一、Issue 详情（/issues/:number）

- **分型**：F 两栏（主列 + 右 metadata 296px sticky）。
- **右栏 9 区**：Assignees/Labels/Projects/Milestone/Development/Notifications/Participants…
- **PureGit（2026-08-09 PageLayout 对齐）**：`right={{node:<aside 5 区/>, width:280, sticky:"nav"}}`。
- **差异**：280 vs 296；缺 New issue 按钮；Projects/Milestone 编辑去杂项。

## 二、PR 详情（/pulls/:number）

- **分型**：F 两栏（右 metadata 320px）。
- **四 tab**：Conversation/Commits/Checks/Files changed（URL 独立 + 计数）。
- **PureGit（2026-08-09 PageLayout 对齐）**：`right={{node:<aside 5 区/>, width:280, sticky:"nav"}}` + 四 tab 单页 state + DiffView。
- **差异**：280 vs 320；缺 Reviewers/Development；四 tab URL 独立 vs state 切换。

## 三、Discussion 详情（/discussions/:number）

- **分型**：F 两栏（主讨论 + 右 sidebar：分类/最活跃/参与）。
- **PureGit（2026-08-09 单列→两栏对齐）**：`right={{node:<aside 分类/投票/参与/>, width:280, sticky:"nav"}}` + 主帖 + 评论 + 表单。
- **差异**：已对齐 ✅。

## 四、新建页

| 页面 | 官方 | PureGit（2026-08-09） |
|------|------|------|
| New Issue | 主列 959 + 右 metadata 296（Projects/CoC 展示） | PageLayout `right={{width:280}}`（Labels/Assignees 选择器） |
| New PR | A 单列居中（compare 两页） | 单列单页合并（已决策） |
| New Discussion | A 单列居中（choose 卡片 → 表单） | 两段式单列（choose 卡片 → `?category=` 表单）✅ |
| New Repository | A 单列居中 | 单列 ✅ |
| New Gist | A 单列 | 单列 ✅ |

## 五、blob 代码页（/blob/:branch/*）

- **分型**：**B+ 三栏**（左文件树 320 + 内容 + 右 Symbols/Outline 344 面板）；树 4 态折叠（三栏/两栏/单栏）。
- **pane 要素**：Collapse file tree + Files + branch + `Search this repository (/)` + Go to file + 树 + **拖拽 splitter**（role=slider）。
- **PureGit（2026-08-09 PageLayout 收编）**：`left={{node:<FileTreeSidebar/>, width:240, lgWidth:320, sticky:"tool", breakpoint:"md", hidden:treeCollapsed}}` + 内容 + 右 w-75 面板（blob 内）。
- **关键**：`hidden:treeCollapsed` 保留 DOM（防重挂载重复请求，2026-08-09 修复）。
- **差异**：缺 Search this repository(/)、拖拽 splitter；面板 300 vs 344；断点 md vs <1024。

## 六、tree 子目录（/tree/:branch/*）

- **分型**：B 两栏（左文件树 + 右内容）。
- **PureGit**：❌ 未包 RepoCode → 无左树（已知问题，见 05-一）。
- **待修**：包 RepoCode（PageLayout left 树）。

## 七、在线编辑（/edit、/new 文件）

- **分型**：B 两栏（左文件树 273 + 编辑区）。
- **编辑区**：header（repo/dirs/文件名 + Commit changes 右）+ 工具条（Indent/Line wrap）+ CM6 + 提交面板。
- **PureGit**：FileEditorPage 手写两栏（240/320）——2026-08-09 已并入 PageLayout left（同 blob 模式）。
- **差异**：断点 md vs <1024。

## 八、Security 告警详情（/security/advisories/GHSA-xxx）

- **分型**：两栏（左 metadata 220 + 右 markdown）。
- **PureGit（2026-08-09 PageLayout 收编）**：`left={{node:<metadata/>, width:220, sticky:"nav"}}` + 右 markdown。
- **差异**：已对齐 ✅（手写 grid 已收编）。

## 九、Actions Run/Job 详情（/actions/runs/:id）

- **分型**：D 两栏（左导航 Summary/All jobs/Run details + 右内容）。
- **Run**：Workflow run summary 卡 + job 卡 + step 耗时。
- **Job**：左导航同 + 右 step 列表 | 日志流双面板 + Search logs。
- **PureGit（2026-08-09 收编 + 单列→两栏对齐）**：RunDetailPage `left={{width:200, sticky:"nav"}}`；JobDetailPage **单列→两栏**（left 200 + Search logs + step 日志展开）。
- **差异**：Job 日志双面板简化为行内展开（去杂项）。

## 十、仓库设置（/settings）

- **分型**：D 两栏（左导航 296 + 右表单区块）。
- **区块**：General + Features + Danger Zone（Visibility/Transfer/Archive/Delete）。
- **PureGit**：**单列**（无左导航，已记录去杂项决策）+ Danger Zone 严格复刻（3 步 dialog + 输入确认）。
- **差异**：无左导航（决策保留）；官方 <768 单列 vs PureGit 全程单列（简化设计）。
