# 05-仓库列表页（tree / Issues / Pulls / Discussions / Releases / Actions / Projects / Wiki / Security / Insights，2026-08-09 重建）

> 统一结论：官方列表页**无 About**（除根 tree）；均已通过 `RepoLayout.hideAbout` 对齐（2026-08-09）。

## 一、文件树页 TreePage（/tree/:branch/*）

- **分型**：B 两栏（左文件树 + 右内容）；根目录 tree = G 型（含 About）。
- **PureGit 现状**：`TreePage` **未包 RepoCode** → 子目录 tree 无左文件树 pane ❌（已知问题）；根 tree About ✅。
- **待修**：tree 子目录包 RepoCode（PageLayout left 树）+ 隐藏 About；根 tree 保留 About。

## 二、Issues 列表（/issues）

- **分型**：C 两栏（左 Issues sidebar navigation 239px 可折叠 + 右列表）。
- **左栏**：Issues/Assigned/Created/Mentioned/Recent + Views/Projects/Milestones/Labels + Collapse sidebar。
- **右**：H1 All issues + New issue + 搜索框 + Author/Labels 下拉 + Open/Closed 计数 tab + 列表行 + 分页。
- **搜索框**：`is:issue state:open`（预填 token）。
- **PureGit（2026-08-09 PageLayout 对齐）**：`left={{node:<filters nav/>, width:260, sticky:"nav"}}` + 右搜索（RepoSearchInput）+ Author/Sort + Open/Closed tab。
- **差异**：无分页；260 vs 239；缺 Views/Milestones/Labels 链接（去杂项）。

## 三、Pull requests 列表（/pulls）

- **分型**：**A 全宽单列**（无 filters 栏）。
- **区块**：H1 "Pull requests: owner/repo" + New pull request + 搜索框 + 过滤条（Author/Label/Projects/Milestones/Reviews/Assignee/Sort 7 项）+ Open/Closed 计数 tab + PR 行 + 分页。
- **搜索框**：`is:pr is:open`（随 state 变）。
- **PureGit**：单列 + PageLayout 不传 side + RepoSearchInput（placeholder `is:pr is:${state}` ✅）+ Author/Sort + Open/Closed tab。
- **差异**：无分页；过滤项 2/7（去杂项）。

## 四、Discussions 列表（/discussions）

- **分型**：两栏（左 Categories 侧栏 + 右列表）。
- **区块**：H1 + New discussion + Pinned + 左 Categories/Most helpful/Community links + 右状态过滤 + 讨论行。
- **搜索框**：`Search all discussions`。
- **PureGit（2026-08-09 全面复刻 + PageLayout 对齐）**：`left={{node:<Categories nav/>, width:280, sticky:"nav"}}` + 搜索/Sort/Filter + 新建两段式 + 分类 URL 化。
- **差异**：基本一致 ✅。

## 五、Releases 列表（/releases）

- **分型**：两栏（左版本列表 + 右 release 卡）。
- **区块**：H1 + 左 sticky 版本 + 右卡（Latest/notes/Assets）+ 空态。
- **PureGit（2026-08-09 PageLayout 对齐）**：`left={{node:<版本 nav/>, width:280, sticky:"nav"}}` + 右卡。
- **差异**：空态简化（官方含创建引导）；About 已隐藏 ✅。

## 六、Actions 列表（/actions）

- **分型**：两栏（左 Workflows 导航 + 右 runs）。
- **左**：Actions/Workflows/**Management**（Caches/Attestations/Usage/Performance）。
- **右**：`Filter workflow runs` 搜索 + 过滤（Workflow/Event/Status/Branch/Actor）+ run 行 + 分页。
- **PureGit（2026-08-09 PageLayout 对齐）**：`left={{node:<Workflows 三分组 nav/>, width:280, sticky:"nav"}}` + 搜索 + 过滤 + runs + Previous/Next。
- **run 行（2026-08-13 重排）**：状态图标（✓/✗/spinner/clock）最左 + 左列标题/元信息（分支 · workflow 名 #号 · by 作者）+ 右列时间/commit sha；状态以纯图标呈现，不再用行尾文字徽标。
- **分页**：复用共享 `Pager` 组件（原手写 Previous/Next 已移除）。
- **过滤（2026-08-13 补 Actor）**：Workflow/Event/Status/Branch/**Actor** 五过滤；Actor 候选从当前 runs 提取触发者去重，后端 `listWorkflowRunsForRepo` `actor` 参数精确过滤。
- **差异**：Management 5 vs 官方 4（Deployments 差异，以 research 为准）。

## 七、Projects 列表（/projects）⭐ 2026-08-09 重建

- **分型**：两栏（左 Issues sidebar navigation + 右 projects 内容）。
- **区块**：H1 Projects → **搜索框 `Search projects`** → Repository projects(h2) → **Open/Closed 状态 tab**（`?state=closed`）→ **Sort: Recently updated** → 项目表格/空态。
- **PureGit（2026-08-09 骨架→官方结构重建）**：H1 + New project + Search projects + Open/Closed tab + Sort + 列表/空态；数据源 legacy REST（404/403/CORS 静默空态）。
- **搜索框**：✅ 已补 `Search projects`。
- **差异**：左 Issues sidebar 未实现（仓库级持久侧栏，可后续统一）；New project 外链官方。

## 八、Wiki（/wiki）

- **分型**：两栏（主列 markdown + Pages 侧栏）。
- **区块**：H1 + Edit/New page + 编辑元信息 + markdown + 页脚（Edit footer + Clone this wiki locally）。
- **PureGit（2026-08-09 PageLayout 对齐）**：`left={{node:<Pages nav/>, width:280, sticky:"nav"}}` + 右 markdown。
- **⚠️ 待二次确认**：Pages 侧栏方向（当前 PureGit 左侧 vs 历史 research/29 官方右侧）。
- **差异**：缺 Edit/New page 按钮、Clone wiki 面板（去杂项）；About 已隐藏 ✅。

## 九、Security（/security）

- **分型**：单列（无侧栏）。
- **区块**：H1 + SECURITY.md 区（渲染/空态 + 编辑）+ Published advisories 列表。
- **PureGit**：单列 + PageLayout 不传 side；详情页左 metadata 220px（PageLayout left）。
- **差异**：About 已隐藏 ✅；详情左列 220（官方 220 ✅）。

## 十、Insights/Pulse（/pulse）

- **分型**：D 两栏（左 Insights 导航 + 右内容）。
- **左**：Pulse/Contributors/Community/Commits/Code frequency/Dependency graph/Network/Forks。
- **右**：H1 + Period 下拉 + Overview 卡 + 分组列表。
- **PureGit（2026-08-09 修复双层 PAGE_SHELL + PageLayout 对齐）**：PageLayout `left={{node:<Insights nav/>, width:240, sticky:"nav"}}` + 右内容（原内层 PAGE_SHELL 嵌套已移除）。
- **差异**：子页置灰（去杂项）；About 已隐藏 ✅。
